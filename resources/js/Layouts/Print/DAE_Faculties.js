import React from 'react';

const DAEFaculties = React.forwardRef((props, ref) => (
    <div ref={ref}>
        <h1>Printable Content</h1>
        <p>This content will be printed.</p>
    </div>
));

export default DAEFaculties;